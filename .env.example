# Environment Configuration for Weight Manager

# Admin user credentials (change these!)
ADMIN_EMAIL=your-admin@example.com
ADMIN_PASSWORD=your-secure-password

# JWT secret for authentication (generate a random string)
JWT_SECRET=your-random-jwt-secret-key-here

# Server configuration
PORT=3000
NODE_ENV=production

# Database configuration (optional - defaults to data/weight_manager.db)
# DB_PATH=custom/path/to/database.db